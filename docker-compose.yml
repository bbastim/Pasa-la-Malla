services:
  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: bomberos
      POSTGRES_PASSWORD: bomberos
      POSTGRES_DB: pasa_la_malla
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
